name: "laughlm-base-evals-v1"
language: "en"
n_grams: 13
find_query_ngrams: false
find_overlap_ngrams: true
index_root: "./work/decont_index"

# Freeze this before production Stage 3. Use valid Lighteval task IDs.
lighteval_tasks: []
custom_lighteval_tasks: null
