# Sealed evaluation is a separate artifact and must never be copied into a
# training Stage-3/Stage-4 repository. Replace placeholders before freezing.
name: "laughlm-sealed-evals-v1"
version: 1
freeze_status: "draft"
repo_id: "REPLACE_WITH_SEALED_EVAL_DATASET_REPO"
revision: "main"
training_repo_id: "REPLACE_WITH_TRAINING_DATASET_REPO"
paths: []
text_column: "text"
