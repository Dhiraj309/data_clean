stage: 1
name: issues_kaggle

# Manifest only. Do not run directly.
enabled: false

disabled_reason: >-
  The issues and kaggle subsets have different schemas and document
  characteristics. Process them independently using issues.yaml and
  kaggle.yaml, then combine them during the final dataset-mixing stage.

source:
  repo_id: "HuggingFaceTB/issues-kaggle-notebooks"
  revision: "main"
  split: "train"
  available_configs:
    - issues
    - kaggle

variants:
  issues: "configs/smol/stage1/issues.yaml"
  kaggle: "configs/smol/stage1/kaggle.yaml"