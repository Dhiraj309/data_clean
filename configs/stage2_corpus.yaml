# Canonical Stage-2 execution plan. Source priority, not list order, determines
# which exact/normalized duplicate survives. Keep this plan as the only normal
# Stage-2 entry point; stage2_process.py remains an explicit recovery tool.
version: 1
stage: 2
dedup_namespace: "laughlm-hq-v2"
sources:
  - config: "stage2/finemath.yaml"
  - config: "stage2/finepdfs_edu.yaml"
  - config: "stage2/stack_edu.yaml"
  - config: "stage2/dolma3_150b.yaml"
  - config: "stage2/fineweb_edu.yaml"
